# ft_transcendence




TODO LIST:
    - replace how you get JWT keys (use vault) in [callback.js, verify.js, auth.js, refresh_token.js]
    - create a script to generate the sensitive environments
    - make backend servers use ssl if it necessary
    - disable debug mode in backend services